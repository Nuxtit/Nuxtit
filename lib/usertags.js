// import { openDB, deleteDB, wrap, unwrap } from 'idb';
// @todo https://www.npmjs.com/package/idb
// @or probably another feathersjs endpoint

export default {
  // store,
  // set: (k, v) => set(k, v, store),
  // get: k => get(k, store),
  // del: k => del(k, store),
  // keys: () => keys(store),
  // clear: () => clear(store),
  set: async (k, v) => null,
  get: async k => null,
  del: async k => null,
  keys: async () => null,
  clear: async () => null,
};
